[build]
buildCommand = "sh -c \"mkdir -p /app/db && pip install -r requirements.txt && cd Exam && python manage.py migrate && python manage.py collectstatic --noinput\""

[deploy]
startCommand = "cd Exam && gunicorn examProject.wsgi --bind 0.0.0.0:$PORT --workers 3 --log-file -"
